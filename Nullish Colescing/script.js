let name = null;
console.log(name ?? "Guest");
console.log(name || "Guest");
// Nullish coalescing operator returns right-hand value only when left-hand value is null or undefined.
let user = {
  name: "Ali",
  age: 20
};

